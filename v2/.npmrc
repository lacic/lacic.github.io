# This project uses the public npm registry.
# Without this, a corporate Artifactory mirror may be inherited from the user
# or global npmrc, which does not serve some of the Astro packages.
registry=https://registry.npmjs.org/
